#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{
    int n;
    scanf("%d",&n);
    int temp=n;
    int sum=0;
    while(temp!=0)
    {
        sum+=temp%10;
        temp/=10;
    }
    printf("%d",sum);
    return 0;
}
